import 'package:flutter/widgets.dart';

class ElCollapseItem extends StatelessWidget {
  const ElCollapseItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
