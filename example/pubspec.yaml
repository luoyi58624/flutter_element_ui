name: example
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_element_ui:
    path: ../packages/flutter_element_ui
  faker: ^2.1.0
  super_sliver_list: ^0.4.1
  decimal: ^3.0.0
  mini_getx: ^5.0.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.10

dependency_overrides:
  luoyi_flutter_base:
    git:
      url: https://github.com/luoyi58624/luoyi_flutter_base.git

flutter:
  uses-material-design: true
