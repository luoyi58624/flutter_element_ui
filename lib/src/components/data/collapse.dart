import 'package:flutter/widgets.dart';
import 'package:flutter_hooks/flutter_hooks.dart';

class ElCollapse extends HookWidget {
  const ElCollapse({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
