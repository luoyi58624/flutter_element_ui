import 'package:flutter/widgets.dart';

class ElCard extends StatelessWidget {
  /// Element UI 卡片组件，它内部直接基于 Material 实现
  const ElCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
