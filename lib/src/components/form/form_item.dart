import 'package:flutter/widgets.dart';

class ElFormItem extends StatelessWidget {
  const ElFormItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
